#include <avr/io.h>
#include <avr/interrupt.h>

int main(void){
	
	DDRB = 0x21;
	PORTB |= ((1 << PINB7) | (1 << PINB0));
	
	TCNT1 = 0x00;
	OCR1A = 15625;
	
	TCCR1B |= (1 << WGM12) | (1 << CS10) | (1 << CS12);
	TIMSK1 |= (1 << OCIE1A);
	
	sei();
	
	while (1) {
	}
}

ISR(TIMER1_COMPA_vect){
	PORTB ^= (1<<PORTB5);
}